import 'package:flutter/material.dart';

class AppColor {
  static const primaryColor = Color(0xFF004D40);
  static const  btnColor = Colors.red;
  static const  bgColor = Colors.teal;
  static const btmNavBarColor = Color(0xFF004D40);
  static const appBarColor = Color(0xFF004D40);
}